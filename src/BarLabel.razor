@inherits Soenneker.Quark.Element

<Div Class="b-bar-label" @attributes="BuildAttributes()" >
    @ChildContent
</Div>

@code {
	public override string? Name { get; set; } = "BarLabel";

    protected override Dictionary<string, object> BuildAttributes()
    {
        Dictionary<string, object> attributes = base.BuildAttributes();

        AppendToClassAttr(attributes, "b-bar-label");

        return attributes;
    }

}
