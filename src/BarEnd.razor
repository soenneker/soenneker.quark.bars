@inherits Soenneker.Quark.Element

<Div Class="navbar-end" @attributes="BuildAttributes()" >
    @ChildContent
</Div>

@code {
	public override string? Name { get; set; } = "BarEnd";

    protected override Dictionary<string, object> BuildAttributes()
    {
        Dictionary<string, object> attributes = base.BuildAttributes();

        AppendToClassAttr(attributes, "navbar-end");

        return attributes;
    }

}
